# The `lang` group of extensions provides languages

## Usage

In your `~/.config/nvim/lua/custom/settings.lua` file, add the following:

```lua
M.extras = {
    -- just a random list of languages, replace!
    "lang.python",
    "lang.c",
    "lang.go",
    "lang.rust",
}
```
